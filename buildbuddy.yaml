actions:
  - name: "Build"
    triggers:
      push:
        branches:
          - "master"
      pull_request:
        branches:
          - "master"
    bazel_commands:
      - "build //Libraries:xcbuild --config=workflows"
